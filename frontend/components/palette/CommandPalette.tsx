import type { CommandPaletteProps } from "@/components/applications/contracts";

export type { CommandPaletteProps };

// Stub: implemented by lane F-onboard/palette. AppShell already mounts it and
// toggles `open` on ⌘K; it renders nothing until then.
export default function CommandPalette(_props: CommandPaletteProps) {
  return null;
}
